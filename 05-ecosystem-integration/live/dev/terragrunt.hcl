# Example Terragrunt live configuration.
# Install Terragrunt, then run from this folder: terragrunt plan
# This example root uses terraform.workspace for the environment, so select
# the dev workspace before planning/applying.

terraform {
  source = "../../"
}
